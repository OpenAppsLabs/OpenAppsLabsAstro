{
  "name": "open-apps-labs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node -e \"require('fs').writeFileSync('dist/.nojekyll','')\"",
    "preview": "astro preview",
    "astro": "astro",
    "predeploy": "node ./scripts/clean-gh-cache.js",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^5.16.7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
