---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';

// Handles the /OpenAppsLabs/ prefix
const baseUrl = import.meta.env.BASE_URL;
---

<Layout title="Privacy Policy | Jotter">
  <div class="container">
    
    <Navbar />

    <main class="policy-layout">
      <header>
        <div class="breadcrumb">Open Apps Labs / Jotter</div>
        <h1>Privacy Policy</h1>
        <div class="meta">Effective: January 8, 2026</div>
      </header>

      <div class="tldr-box">
        <strong>Short Version:</strong> Your notes are yours. Jotter is an offline-first app. 
        I do not have access to your notes, passwords, or any content you create within the app.
      </div>

      <article class="content">
        <section>
          <h2>01. Data Storage</h2>
          <p>
            <strong>Jotter</strong> operates on an "Offline First" principle.
          </p>
          <ul>
            <li>
              <strong>Local Database:</strong> 
              <span class="muted">All notes and folders are stored in a local SQLite (Room) database on your device.</span>
            </li>
            <li>
              <strong>No Cloud Sync:</strong> 
              <span class="muted">This app does not sync data to any cloud server. If you uninstall the app without backing up, your data is removed by the Android OS.</span>
            </li>
          </ul>
        </section>

        <section>
          <h2>02. Permissions</h2>
          <p>The app requests minimal permissions to function:</p>
          <ul>
            <li>
              <strong>BIOMETRIC_ID</strong>
              <span class="muted">Used solely to unlock secured notes. Biometric data is handled by the Android System and is never accessed by the app directly.</span>
            </li>
            <li>
              <strong>READ/WRITE STORAGE</strong>
              <span class="muted">Required only if you use the Backup & Restore feature to save a copy of your notes.</span>
            </li>
          </ul>
        </section>

        <section>
          <h2>03. Analytics</h2>
          <p>
            I do not use Google Analytics, Firebase, or any other tracking software. I do not know how many notes you have or what you write.
          </p>
        </section>

        <section>
          <h2>04. Contact</h2>
          <p>
            Questions? <a href="mailto:openappslabs@gmail.com" class="link-highlight">openappslabs@gmail.com</a>
          </p>
        </section>
      </article>
    </main>

    <Footer />
  </div>
</Layout>

<style>
  /* Styling consistent with your other policy pages */
  .policy-layout {
    max-width: 800px;
    margin: 60px auto;
    padding: 0 20px;
  }

  .breadcrumb {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 10px;
    text-transform: uppercase;
  }

  .meta {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--accent-red);
    margin-top: 10px;
  }

  .tldr-box {
    background: rgba(255, 255, 255, 0.05);
    border-left: 3px solid var(--accent-red);
    padding: 20px;
    margin: 40px 0;
    font-family: var(--font-inter);
    border-radius: 4px;
  }

  .content section {
    margin-bottom: 40px;
  }

  h2 {
    font-family: var(--font-mono);
    font-size: 1.2rem;
    margin-bottom: 15px;
    color: var(--text-primary);
  }

  p, li {
    line-height: 1.6;
    color: var(--text-secondary);
    margin-bottom: 12px;
  }

  .muted {
    display: block;
    font-size: 0.9rem;
    opacity: 0.7;
    margin-top: 4px;
  }

  .link-highlight {
    color: var(--text-primary);
    text-decoration: underline;
  }

  ul {
    list-style: none;
    padding-left: 0;
  }

  li::before {
    content: "> ";
    color: var(--accent-red);
    font-family: var(--font-mono);
    font-weight: bold;
  }
</style>